# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: rancher
  namespace: cattle-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: rancher-card-data
    namespace: cattle-system
  widgetData:
    title: Rancher
    headerLeft: Install Rancher via its official Helm chart.
    tooltip: Rancher blueprint
    icon:
      name: fa-cow
      color: grey
    tags:
      - cluster-management
      - kubernetes
      - rancher
    extra: ''
    items: []
    footer:
      - resourceRefId: rancher-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-cow", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: rancher-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: rancher-install
        namespace: cattle-system
        resource: buttons
        verb: GET
