apiVersion: v2
# NOTE: the chart name becomes the Composition Kind (rancher-installer -> RancherInstaller).
# It MUST differ from the top-level values key of any wrapped subchart (here: `rancher`),
# otherwise crdgen produces a self-referential `spec.rancher.spec` and CRD generation fails.
name: rancher-installer
description: A Krateo blueprint that installs Rancher via the official Helm chart. Each Composition created from this blueprint acts as a Rancher installer. cert-manager is a required prerequisite and must be installed separately, beforehand (see README).

# A chart can be either an 'application' or a 'library' chart.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/).
# NOTE: this drives the Composition CRD version (e.g. 0.1.0 -> v0-1-0).
version: CHART_VERSION

# This is the version of Rancher being deployed. Kept in sync with the rancher subchart below.
appVersion: "v2.14.2"

# The blueprint wraps the upstream Rancher chart. Its values are passed under the `rancher`
# key in values.yaml. cert-manager is NOT bundled: it is a required prerequisite and must be
# installed separately before this composition (see README) — it cannot live in the same Helm
# release as Rancher because Rancher's Issuer needs the cert-manager CRDs to already exist.
dependencies:
  - name: rancher
    version: "2.14.2"
    repository: "https://releases.rancher.com/server-charts/stable"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - rancher
  - cluster-management
  - installer
sources:
  - https://github.com/krateoplatformops-blueprints/rancher
annotations:
  krateoSupportedVersion: "2.7.0"
